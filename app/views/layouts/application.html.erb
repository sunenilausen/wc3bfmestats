<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "WC3BFMEStats" %> [BETA]<%= " (dev)" if Rails.env.development? %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="WC3BFMEStats">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <nav class="bg-white border-b border-gray-200 fixed w-full z-10 top-0 left-0 shadow">
      <div class="container mx-auto flex flex-wrap items-center justify-between px-4 py-3">
      <a href="/" class="text-2xl font-bold text-blue-700">WC3BFMEStats <span class="text-xs font-normal bg-yellow-400 text-yellow-900 px-1.5 py-0.5 rounded">BETA</span><%= " (dev)" if Rails.env.development? %></a>
      <ul class="flex space-x-8">
        <li>
          <%= form_with url: players_path, method: :get, local: true, class: "flex items-center" do %>
            <%= text_field_tag :search, params[:search], placeholder: "Search player...", class: "text-sm border-gray-300 rounded-md shadow-sm focus:border-blue-300 focus:ring focus:ring-blue-200 focus:ring-opacity-50 py-1 px-2 w-32" %>
          <% end %>
        </li>
        <li>
          <%= link_to players_path, class: "text-gray-700 hover:text-blue-600 font-semibold px-4 py-2 rounded transition-colors duration-200" do %>
            Players <span class="text-xs text-gray-400">(<%= Player.joins(:matches).where(matches: { ignored: false }).distinct.count %>)</span>
          <% end %>
        </li>
        <li>
          <%= link_to matches_path, class: "text-gray-700 hover:text-blue-600 font-semibold px-4 py-2 rounded transition-colors duration-200" do %>
            Matches <span class="text-xs text-gray-400">(<%= Match.where(ignored: false).count %>)</span>
          <% end %>
        </li>
        <li>
          <%= link_to "Factions", factions_path, class: "text-gray-700 hover:text-blue-600 font-semibold px-4 py-2 rounded transition-colors duration-200" %>
        </li>
        <li>
          <%= link_to "Statistics", statistics_path, class: "text-gray-700 hover:text-blue-600 font-semibold px-4 py-2 rounded transition-colors duration-200" %>
        </li>
        <li>
          <%= link_to "Lobbies", lobbies_path, class: "text-gray-700 hover:text-blue-600 font-semibold px-4 py-2 rounded transition-colors duration-200" %>
        </li>
        <li>
          <%= link_to "Forum", "https://wc3bfme.net", target: "_blank", class: "text-gray-700 hover:text-blue-600 font-semibold px-4 py-2 rounded transition-colors duration-200" %>
        </li>
        <% if current_user&.admin? %>
          <li>
            <%= link_to "Admin", admin_root_path, class: "text-gray-700 hover:text-blue-600 font-semibold px-4 py-2 rounded transition-colors duration-200" %>
          </li>
        <% end %>
      </ul>
      </div>
    </nav>

    <% if RatingRecalculationStatus.in_progress? %>
      <div class="fixed top-16 left-0 right-0 z-20 bg-yellow-100 border-b border-yellow-300 px-4 py-2 text-center text-yellow-800">
        <span class="inline-block animate-spin mr-2">&#8635;</span>
        Ratings are being recalculated. Player ratings may be temporarily inaccurate.
      </div>
    <% end %>

    <main class="container mx-auto mt-28 px-5 flex <%= 'mt-36' if RatingRecalculationStatus.in_progress? %>">
      <%= yield %>
    </main>
  </body>
</html>
